# PC와 휴대폰이 같은 Wi-Fi에 있어야 합니다.
# 설정하지 않으면 Expo LAN 모드( npm run mobile )에서 Metro IP로 API 주소를 자동 추론합니다.
# 수동 지정 시 PC의 LAN IPv4를 넣으세요 (Windows: ipconfig).
# EXPO_PUBLIC_API_URL=http://192.168.0.10:4000/api/v1
